# Upstream Strategy

The preferred sequence is:

1. Solve through package-level customization.
2. Propose missing extension points upstream when the gap is general.
3. Keep only minimal temporary workarounds locally.
