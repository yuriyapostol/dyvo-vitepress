# Architecture

The repository hosts both:

- the reusable package source under `src/`;
- the package documentation site under `docs/`.

The package should stay thin at runtime and expose only documented entry points.
