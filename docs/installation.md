# Installation

The package may be consumed locally at first through a `file:` dependency and later through a GitHub git dependency pinned to a tag.

When preparing a tagged release, run `npm run build` in this repository first so `dist/` contains the current config-safe runtime helpers.
