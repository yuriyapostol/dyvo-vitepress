# Початок роботи

`dyvo-vitepress-theme` — це повторно використовуваний пакет для сайтів на базі VitePress.

Пакет має надавати:

- ядро теми;
- повторно використовувані фічі;
- спільні конфігураційні утиліти.
