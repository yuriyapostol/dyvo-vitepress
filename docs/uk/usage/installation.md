# Встановлення

Спершу пакет можна підключати локально через залежність `file:`, а пізніше через GitHub git dependency, зафіксовану на тегу.

Перед підготовкою тегованого релізу в цьому репозиторії треба виконати `npm run build`, щоб у `dist/` були актуальні config-safe runtime helper-и.
