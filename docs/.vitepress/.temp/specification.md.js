import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"dyvo-vitepress: specification","description":"","frontmatter":{},"headers":[],"relativePath":"specification.md","filePath":"specification.md"}');
const _sfc_main = { name: "specification.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="dyvo-vitepress-specification" tabindex="-1">dyvo-vitepress: specification <a class="header-anchor" href="#dyvo-vitepress-specification" aria-label="Permalink to &quot;dyvo-vitepress: specification&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Draft.</p><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p><code>@yuriyapostol/dyvo-vitepress</code> is a reusable package for VitePress-based sites. It should provide:</p><ul><li>a reusable theme layer on top of VitePress;</li><li>reusable UI components and content features;</li><li>shared configuration helpers for SEO, metadata, and content conventions;</li><li>a stable integration surface for multiple sites.</li></ul><p>The package is intended to be used by <code>dyvoshkola.github.io</code> first, but it must not be tightly coupled to that single site.</p><h2 id="main-goals" tabindex="-1">Main goals <a class="header-anchor" href="#main-goals" aria-label="Permalink to &quot;Main goals&quot;">​</a></h2><ol><li>Extract reusable VitePress customizations from <code>dyvoshkola.github.io</code> into a standalone package.</li><li>Preserve the ability to update upstream VitePress regularly.</li><li>Keep local customizations working while upstream pull requests are still open.</li><li>Allow reuse of the same package across multiple sites.</li><li>Keep site-specific content, navigation, and branding outside of the package by default.</li></ol><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><ul><li>Do not fork VitePress by default.</li><li>Do not move site content into this repository.</li><li>Do not hardcode one specific site structure unless explicitly documented as a feature contract.</li><li>Do not expose internal implementation details as public API unless they are intended to be supported long-term.</li></ul><h2 id="architectural-principles" tabindex="-1">Architectural principles <a class="header-anchor" href="#architectural-principles" aria-label="Permalink to &quot;Architectural principles&quot;">​</a></h2><h3 id="_1-layered-design" tabindex="-1">1. Layered design <a class="header-anchor" href="#_1-layered-design" aria-label="Permalink to &quot;1. Layered design&quot;">​</a></h3><p>The package should be split into three logical layers:</p><ul><li><code>theme core</code>: layout, navigation primitives, sidebar primitives, shared styles, theme config types;</li><li><code>features</code>: optional and reusable content-oriented extensions such as comments and news;</li><li><code>helpers</code>: shared utilities for SEO, metadata derivation, and configuration assembly.</li></ul><h3 id="_2-public-api-first" tabindex="-1">2. Public API first <a class="header-anchor" href="#_2-public-api-first" aria-label="Permalink to &quot;2. Public API first&quot;">​</a></h3><p>Consumers should use only documented exports from the package root. Internal files and folders may change without being treated as breaking API.</p><h3 id="_3-site-specific-data-stays-outside" tabindex="-1">3. Site-specific data stays outside <a class="header-anchor" href="#_3-site-specific-data-stays-outside" aria-label="Permalink to &quot;3. Site-specific data stays outside&quot;">​</a></h3><p>The package may define schemas and helpers for configuration, but actual site data should remain in the consuming project:</p><ul><li>navigation trees;</li><li>sidebar trees;</li><li>site URL;</li><li>brand copy and labels;</li><li>external service IDs and repository names;</li><li>content collections.</li></ul><h3 id="_4-upstream-compatibility-matters" tabindex="-1">4. Upstream compatibility matters <a class="header-anchor" href="#_4-upstream-compatibility-matters" aria-label="Permalink to &quot;4. Upstream compatibility matters&quot;">​</a></h3><p>The package should prefer VitePress public APIs. Imports from <code>vitepress/dist/...</code> are allowed only as an explicit temporary compatibility tradeoff and should be tracked as technical debt.</p><h2 id="initial-scope" tabindex="-1">Initial scope <a class="header-anchor" href="#initial-scope" aria-label="Permalink to &quot;Initial scope&quot;">​</a></h2><h3 id="theme-core" tabindex="-1">Theme core <a class="header-anchor" href="#theme-core" aria-label="Permalink to &quot;Theme core&quot;">​</a></h3><p>The initial <code>theme core</code> is expected to contain:</p><ul><li>custom <code>Layout</code> wrapper;</li><li>local navigation;</li><li>sidebar components;</li><li>shared badges and related theme UI components;</li><li>base CSS customizations;</li><li>theme config typings.</li></ul><h3 id="features" tabindex="-1">Features <a class="header-anchor" href="#features" aria-label="Permalink to &quot;Features&quot;">​</a></h3><p>The initial <code>features</code> scope is expected to contain:</p><ul><li><code>comments</code>: Giscus-based comments integration;</li><li><code>news</code>: listing, archive links, metadata rendering, and related utilities.</li></ul><p>The <code>news</code> feature may depend on a documented content contract, including a directory convention such as <code>news/**/*.md</code>, if that contract is explicitly declared in the documentation.</p><p>Current decision:</p><ul><li><code>comments</code> and <code>news</code> should be disabled by default and enabled explicitly by the consuming site.</li><li><code>news</code> stays in the main package for now. Renaming or extraction may be revisited later if the package surface changes.</li></ul><h3 id="helpers" tabindex="-1">Helpers <a class="header-anchor" href="#helpers" aria-label="Permalink to &quot;Helpers&quot;">​</a></h3><p>The initial helper scope is expected to contain:</p><ul><li>fallback metadata extraction from markdown files;</li><li>page identifier derivation;</li><li>SEO head helpers;</li><li>locale helpers;</li><li>config composition helpers.</li></ul><h2 id="proposed-repository-structure" tabindex="-1">Proposed repository structure <a class="header-anchor" href="#proposed-repository-structure" aria-label="Permalink to &quot;Proposed repository structure&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>  index.ts</span></span>
<span class="line"><span>  config/</span></span>
<span class="line"><span>    createConfig.ts</span></span>
<span class="line"><span>  theme/</span></span>
<span class="line"><span>    index.ts</span></span>
<span class="line"><span>    config.ts</span></span>
<span class="line"><span>    styles/</span></span>
<span class="line"><span>      base.css</span></span>
<span class="line"><span>    components/</span></span>
<span class="line"><span>      Layout.vue</span></span>
<span class="line"><span>      LocalNav.vue</span></span>
<span class="line"><span>      Sidebar.vue</span></span>
<span class="line"><span>      SidebarGroup.vue</span></span>
<span class="line"><span>      SidebarItem.vue</span></span>
<span class="line"><span>      DyvoBadge.vue</span></span>
<span class="line"><span>      DyvoUserBadge.vue</span></span>
<span class="line"><span>    internals/</span></span>
<span class="line"><span>      vitepress-internals.d.ts</span></span>
<span class="line"><span>  features/</span></span>
<span class="line"><span>    comments/</span></span>
<span class="line"><span>      index.ts</span></span>
<span class="line"><span>      config.ts</span></span>
<span class="line"><span>      Comments.vue</span></span>
<span class="line"><span>      giscus-light-theme.css</span></span>
<span class="line"><span>      giscus-dark-theme.css</span></span>
<span class="line"><span>    news/</span></span>
<span class="line"><span>      index.ts</span></span>
<span class="line"><span>      config.ts</span></span>
<span class="line"><span>      utils.ts</span></span>
<span class="line"><span>      NewsList.vue</span></span>
<span class="line"><span>      NewsMeta.vue</span></span>
<span class="line"><span>      NewsMetaAuthors.vue</span></span>
<span class="line"><span>      NewsMetaPublishedAt.vue</span></span>
<span class="line"><span>      NewsMetaStatuses.vue</span></span>
<span class="line"><span>      NewsArchiveLinks.vue</span></span>
<span class="line"><span>  shared/</span></span>
<span class="line"><span>    i18n.ts</span></span>
<span class="line"><span>    seo.ts</span></span>
<span class="line"><span>    page-metadata.ts</span></span></code></pre></div><h2 id="proposed-public-exports" tabindex="-1">Proposed public exports <a class="header-anchor" href="#proposed-public-exports" aria-label="Permalink to &quot;Proposed public exports&quot;">​</a></h2><p>The package root should eventually expose only a small documented API:</p><ul><li><code>theme</code></li><li><code>createConfig</code></li><li><code>defineThemeConfig</code></li><li>selected reusable Vue components only if they are meant for direct consumer use</li></ul><p>Helper intent:</p><ul><li><code>defineThemeConfig</code> should be a typed helper for the package-specific <code>themeConfig</code> shape.</li><li><code>createConfig</code> should be a small factory for assembling the final VitePress config with package defaults and shared config logic when that becomes necessary.</li></ul><p>Current decision:</p><ul><li>Keep the export surface minimal and use <code>theme</code>, <code>createConfig</code>, and <code>defineThemeConfig</code> as the primary package API.</li></ul><p>Possible entry points:</p><ul><li><code>@yuriyapostol/dyvo-vitepress</code></li><li><code>@yuriyapostol/dyvo-vitepress/theme</code></li><li><code>@yuriyapostol/dyvo-vitepress/features/news</code></li><li><code>@yuriyapostol/dyvo-vitepress/features/comments</code></li></ul><p>Subpath exports should be added only when there is a real consumer need.</p><h2 id="consumption-model" tabindex="-1">Consumption model <a class="header-anchor" href="#consumption-model" aria-label="Permalink to &quot;Consumption model&quot;">​</a></h2><p>A consuming VitePress site should stay thin.</p><p>Expected responsibilities of the consuming site:</p><ul><li>content files;</li><li><code>.vitepress/config.*</code> as a site assembly layer;</li><li>site navigation and sidebar data;</li><li>feature configuration values;</li><li>brand assets and brand text unless intentionally shared.</li></ul><p>Expected responsibilities of the package:</p><ul><li>reusable theme implementation;</li><li>reusable feature implementation;</li><li>config helpers and types;</li><li>stable defaults where appropriate.</li></ul><h2 id="documentation-strategy" tabindex="-1">Documentation strategy <a class="header-anchor" href="#documentation-strategy" aria-label="Permalink to &quot;Documentation strategy&quot;">​</a></h2><p>Yes, this repository may contain its own documentation site built with VitePress, including examples that use the package itself. This is a good fit for this project.</p><p>That documentation should serve several purposes:</p><ul><li>package usage guide;</li><li>API reference;</li><li>feature contracts such as required content structure for <code>news</code>;</li><li>development notes for maintainers;</li><li>visual sandbox for testing the theme and components.</li></ul><h2 id="will-documentation-make-the-package-heavier" tabindex="-1">Will documentation make the package heavier? <a class="header-anchor" href="#will-documentation-make-the-package-heavier" aria-label="Permalink to &quot;Will documentation make the package heavier?&quot;">​</a></h2><p>Not if the repository is structured correctly.</p><p>Documentation should not be shipped to consuming projects as runtime package payload. To ensure that:</p><ul><li>source package code should live under <code>src/</code>;</li><li>documentation should live under <code>docs/</code>;</li><li>docs-only dependencies should stay in <code>devDependencies</code>;</li><li>published package contents should be restricted with <code>files</code> in <code>package.json</code>;</li><li>package <code>exports</code> should expose only package entry points, not <code>docs/*</code>;</li><li>build output for the package should be separate from the docs build output.</li></ul><p>In other words, one repository may host both:</p><ul><li>the reusable npm package;</li><li>its own VitePress documentation site.</li></ul><p>But consumers should install only the published package artifacts, not the docs site.</p><h2 id="recommended-documentation-layout" tabindex="-1">Recommended documentation layout <a class="header-anchor" href="#recommended-documentation-layout" aria-label="Permalink to &quot;Recommended documentation layout&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>docs/</span></span>
<span class="line"><span>  index.md</span></span>
<span class="line"><span>  getting-started.md</span></span>
<span class="line"><span>  architecture.md</span></span>
<span class="line"><span>  usage/</span></span>
<span class="line"><span>    installation.md</span></span>
<span class="line"><span>    configuration.md</span></span>
<span class="line"><span>    theme.md</span></span>
<span class="line"><span>    comments.md</span></span>
<span class="line"><span>    news.md</span></span>
<span class="line"><span>  development/</span></span>
<span class="line"><span>    local-development.md</span></span>
<span class="line"><span>    upstream-strategy.md</span></span></code></pre></div><p>The docs site may use the package itself for dogfooding. That is desirable as long as package build and docs build remain separate concerns.</p><h2 id="packaging-constraints" tabindex="-1">Packaging constraints <a class="header-anchor" href="#packaging-constraints" aria-label="Permalink to &quot;Packaging constraints&quot;">​</a></h2><p>The package should be safe to consume without dragging unnecessary files.</p><p>Requirements:</p><ul><li><code>package.json</code> should use <code>files</code> to limit published artifacts;</li><li>built output should go to <code>dist/</code>;</li><li>docs build output should go to a separate location such as <code>.vitepress/dist</code> inside <code>docs/</code> and should not be published as package runtime code;</li><li><code>peerDependencies</code> should be considered for <code>vitepress</code>, <code>vue</code>, and related tooling if that gives cleaner integration;</li><li>the package should document compatible VitePress version ranges.</li></ul><h2 id="upstream-strategy" tabindex="-1">Upstream strategy <a class="header-anchor" href="#upstream-strategy" aria-label="Permalink to &quot;Upstream strategy&quot;">​</a></h2><p>Default strategy:</p><ol><li>Implement through package-level theme customization first.</li><li>If VitePress public API is insufficient, identify the missing extension point.</li><li>Submit an upstream PR when the improvement is generally useful.</li><li>Keep a temporary local workaround until the upstream change is available.</li><li>Use a minimal fork or patch only when the workaround cannot be expressed cleanly in the package.</li></ol><h2 id="initial-migration-plan" tabindex="-1">Initial migration plan <a class="header-anchor" href="#initial-migration-plan" aria-label="Permalink to &quot;Initial migration plan&quot;">​</a></h2><ol><li>Create package skeleton in this repository.</li><li>Move reusable code from <code>dyvoshkola.github.io/.vitepress/theme</code> into <code>src/theme</code>.</li><li>Move reusable code from <code>dyvoshkola.github.io/.vitepress/features</code> into <code>src/features</code>.</li><li>Move shared helpers into <code>src/shared</code> and <code>src/config</code>.</li><li>Replace direct site implementation with package consumption.</li><li>Stabilize package API and document feature contracts.</li><li>Reduce reliance on <code>vitepress/dist/...</code> over time.</li></ol><h2 id="current-decisions" tabindex="-1">Current decisions <a class="header-anchor" href="#current-decisions" aria-label="Permalink to &quot;Current decisions&quot;">​</a></h2><ul><li>The initial stable public API should use <code>theme</code>, <code>createConfig</code>, and <code>defineThemeConfig</code>.</li><li><code>comments</code> and <code>news</code> should be opt-in.</li><li><code>news</code> should remain inside the main package for now.</li><li>The package should be consumed locally first through a <code>file:</code> dependency.</li><li>After local integration stabilizes, the consuming site should switch to a GitHub git dependency pinned to a specific version reference such as a tag or commit SHA.</li><li>The package should officially support the same VitePress minor line as the consuming site, currently <code>^1.6.4</code>.</li></ul><h2 id="immediate-next-step" tabindex="-1">Immediate next step <a class="header-anchor" href="#immediate-next-step" aria-label="Permalink to &quot;Immediate next step&quot;">​</a></h2><p>Use this specification as the source of truth for scaffolding the package and for extracting code from <code>dyvoshkola.github.io</code> into <code>dyvo-vitepress</code>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("specification.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const specification = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  specification as default
};
